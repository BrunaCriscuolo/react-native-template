import { Colors } from 'src/styles';

const { gray100, gray500, gray600, gray900 } = Colors;

export default {
  dark: true,
  colors: {
    fonts: {
      primary: gray100,
      secondary: gray500,
      disabled: gray600,
    },
  },
};
